# Wildcard: bitforge and any *.bitforge resolve to this server's LAN IP.
address=/bitforge/192.168.0.165

# Forward everything else to the existing router/ISP DNS.
server=192.168.0.1

no-hosts
no-resolv
bind-interfaces
listen-address=0.0.0.0
