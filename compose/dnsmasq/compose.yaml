services:
  dnsmasq:
    image: 4km3/dnsmasq
    container_name: dnsmasq
    restart: unless-stopped

    cap_add:
      - NET_ADMIN

    ports:
      - "192.168.0.165:53:53/udp"
      - "192.168.0.165:53:53/tcp"

    volumes:
      - ./config/dnsmasq.conf:/etc/dnsmasq.conf:ro
